export const metadata = {
  title: "公开提示词库",
  description: "浏览社区贡献的优质提示词，按角色与关键词搜索。",
  alternates: { canonical: "/public" },
  robots: { index: true, follow: true },
};

export default function PublicLayout({ children }) {
  return children;
}
